version: 0.2

phases:
  build:
    commands:
    - git branch | sed -n -e 's/^\* \(.*\)/\1/p'
    - python helloworld.py
    - branch=$(git branch | sed -n -e 's/^\* \(.*\)/\1/p')
    - echo $branch
artifacts:
  files:
    - '**/*'
  name: myname-$(date +%Y-%m-%d)
