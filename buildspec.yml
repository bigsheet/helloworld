version: 0.2

env:
  variables:
phases:
  build:
    commands:
    - pwd
    - ls
    - git name-rev --name-only $(git rev-parse HEAD)
    - python helloworld.py
  post_build:
    commands:
artifacts:
  files:
    - '**/*'
  name: myname-$(date +%Y-%m-%d)
